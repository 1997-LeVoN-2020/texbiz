---
name: package-for-deploy
description: Пересобрать сайт ТЕХБИЗ и упаковать версионированные texbiz-site-v<VERSION>.zip / texbiz-pyapp-v<VERSION>.zip для загрузки на хостинг (reg.ru, ISPmanager). Использовать когда просят "собери архивы для деплоя", "подготовь zip для загрузки на хостинг", "запакуй новую версию".
---

## Команда

```
python package.py
```

Собирает сайт (`build.py`) и упаковывает `releases/texbiz-site-v<VERSION>.zip` + `releases/texbiz-pyapp-v<VERSION>.zip`. `<VERSION>` берётся из файла `VERSION` в корне репозитория.

`package.py` **откажется** собирать архив, если файлы с текущей версией уже есть в `releases/` — старые версии никогда не перезаписываются и не удаляются. Перед новой сборкой нужно поднять версию в `VERSION` (семвер, например `1.0.0` → `1.0.1`/`1.1.0`).

`releases/` в `.gitignore`, архивы не коммитятся — но локально накапливается история версий для отката. Тот же паттерн (`VERSION` + отказ от перезаписи + `releases/`), что и в проекте модуля бронирования (`booking-module`, `build_release.sh`) — только там один архив из `git archive HEAD`, а здесь два (сайт собирается заново из `dist/`, pyapp — из `src/pyapp/`).

## Куда что распаковывать (важно для этого хостинга)

- **`texbiz-site-v<VERSION>.zip`** (содержимое `dist/`, без обёрточной папки) → напрямую в директорию домена в ISPmanager (`/var/www/<пользователь>/data/www/tex-biz.ru`).
- **`texbiz-pyapp-v<VERSION>.zip`** (содержимое `src/pyapp/`) → в подпапку `mailapp/` **внутри той же** директории домена — на этом тарифе отдельной директории для Python-приложений нет, поэтому статика и Python-приложение физически лежат в одном корне (см. README, раздел «2. Python-приложение для формы»).

## После загрузки

Свериться, что `MAIL_ENDPOINT` в `src/static/script.js` (по умолчанию `/mailapp/send`) совпадает с реальным путём Python-приложения, настроенным в ISPmanager. Если путь другой — поменять константу в `script.js`, поднять `VERSION` и пересобрать (`python package.py`).
