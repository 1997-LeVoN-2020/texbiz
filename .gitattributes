# Хранится в git, но не нужно на сервере — исключается из `git archive`
# (скрипт сборки релиза), чтобы список того, что едет на хостинг, был один.
.claude/ export-ignore
.gitattributes export-ignore
.gitignore export-ignore
README.md export-ignore
docs/ export-ignore
scripts/ export-ignore
build_release.sh export-ignore
