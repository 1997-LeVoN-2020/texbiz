Django==6.1.1

# Django 6.1 требует SQLite 3.37 и новее. На хостинге reg.ru системный SQLite —
# 3.26, и без этого пакета сайт там не запускается: manage.py падает на любой
# работе с базой. Пакет приносит свой SQLite (3.51), настройки подменяют им
# системный модуль. Ставится только на Linux: под Windows готовых сборок нет,
# да и системный SQLite там свежий.
pysqlite3-binary==0.5.4.post2; sys_platform == "linux"
