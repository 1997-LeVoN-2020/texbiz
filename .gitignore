*.zip
.DS_Store
Thumbs.db

# build output — generated by `python build.py`, not source
/dist/

# versioned deploy archives — generated by `python package.py`, kept
# locally across versions (see VERSION), not committed
/builds/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Claude Code local settings — machine-specific, not shared project config
.claude/settings.local.json
