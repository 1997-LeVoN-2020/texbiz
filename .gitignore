*.zip
.DS_Store
Thumbs.db

# versioned deploy archives — generated by `bash build_release.sh`, kept
# locally across versions (see VERSION), not committed
/releases/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Claude Code local settings — machine-specific, not shared project config
.claude/settings.local.json

# Real hosting SFTP/SSH connection details — machine-specific secrets,
# never committed. Copy deploy.env.example to deploy.env and fill it in.
deploy.env
